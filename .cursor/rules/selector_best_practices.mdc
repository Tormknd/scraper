---
alwaysApply: false
---
  - "**/extractors/*.py"
filters:
  - type: content
    pattern: "select\\(\".*\\")"
actions:
  - type: execute
    command: |
      echo "Vérifier que le sélecteur n’utilise pas de classes dynamiques"
  - type: suggest
    message: |
      1. Préférez XPath ou sélecteurs basés sur des attributs stables (id, data-…).
      2. Évitez les classes auto-générées et les index statiques.